spring.application.name=COGNITIVE-ASSESSMENT
server.port=8098

# --- H2 (commenté si tu utilises PostgreSQL) ---
# spring.datasource.url=jdbc:h2:mem:cognitive_assessment;DB_CLOSE_DELAY=-1
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.h2.console.enabled=true
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# --- PostgreSQL (base cognitive_assessment déjà créée) ---
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/cognitive_assessment}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:123}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=... (auto-détecté par Spring si absent)

# --- Email Configuration ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=benkhalifatasnim70@gmail.com
spring.mail.password=detu kyza ujhb lgiz

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# --- Notification Settings ---
app.notification.email=benkhalifatasnim70@gmail.com

eureka.client.service-url.defaultZone=${EUREKA_SERVER_URL:http://localhost:8761/eureka}


management.endpoints.web.exposure.include=*
management.prometheus.metrics.export.enabled=true

springdoc.api-docs.path=/cognitive/v3/api-docs
