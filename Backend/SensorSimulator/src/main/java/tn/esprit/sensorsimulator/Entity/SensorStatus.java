package tn.esprit.sensorsimulator.Entity;

public enum SensorStatus {
    ACTIVE,       // Sensor is operational and generating data
    INACTIVE,     // Sensor is turned off / not in use
    MALFUNCTION   // Sensor is reporting errors
}
