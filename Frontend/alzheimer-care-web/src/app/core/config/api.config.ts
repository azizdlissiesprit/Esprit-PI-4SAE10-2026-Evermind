/**
 * URL de base de l'API Formation.
 * En dev (ng serve), le proxy proxy.conf.json redirige /api/formation vers http://localhost:9086/formation.
 * Lancez le backend avec: .\run-backend.cmd (à la racine du projet)
 */
export const FORMATION_API_BASE = '/api/formation';
